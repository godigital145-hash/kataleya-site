---
import ProduitPage from "../../components/pages/produit";
import Layout from "../../layouts/Layout.astro";

export function getStaticPaths() {
    return [{ params: { id: "_" } }];
}

const { id } = Astro.params;
---

<Layout>
    <ProduitPage id={id ?? ""} client:load />
</Layout>
